# Hello World — React + Sass

A simple React app with a blue background and red text, styled with SCSS.

## Setup
```bash
npm install
npm start
```

## Files
- `src/App.js` — Main component
- `src/App.scss` — SCSS styles (blue background, red text)
- `src/index.js` — Entry point
